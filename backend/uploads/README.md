# Uploads Directory

This directory stores user-uploaded images for issue reports.

## Structure
- `images/` - Issue report images

The application automatically creates this directory structure if it doesn't exist.

## Permissions
Make sure this directory has write permissions:
```bash
chmod -R 755 uploads/
```
